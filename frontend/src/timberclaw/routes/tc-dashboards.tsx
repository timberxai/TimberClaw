import { Card, Typography } from "antd";

export default function TcDashboards() {
  return (
    <Card title="仪表盘（占位）">
      <Typography.Paragraph>
        按 PRD §8.3.1，仪表盘将与列表、表单、详情并列为一等页面；此处预留路由与
        Ant Design 壳，后续接入 Ant Design Charts 与后端聚合接口。
      </Typography.Paragraph>
    </Card>
  );
}
